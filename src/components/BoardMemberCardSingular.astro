---
interface Props {
  title: string;
  fullName: string;
  position: string;
  imageUrl: string;
  linkedinUrl?: string;
  googleScholarUrl?: string;
  githubUrl?: string;
  isW?: boolean;
}

const {
  title,
  fullName,
  position,
  imageUrl,
  linkedinUrl,
  googleScholarUrl,
  githubUrl,
  isW,
} = Astro.props;

import { getAssetUrl } from "../utils/cdn";
---

<div
  class="relative w-full aspect-[1.6] rounded-lg bg-white overflow-hidden flex items-end justify-center"
>
  <!-- Background Pattern -->
  <div
    class="absolute inset-0 pointer-events-none opacity-50 z-0"
    style={`background-image: url('${getAssetUrl("/noise.webp")}'); background-size: cover;`}
  >
  </div>
  <div
    class="w-[97%] h-[96.5%] flex justify-center items-center absolute z-20 pointer-events-none"
  >
    <img
      src={isW
        ? getAssetUrl("/casette-layout-pink.webp")
        : getAssetUrl("/casette-layout-orange.webp")}
      alt="Background Pattern"
      class="absolute w-full h-full object-cover"
      loading="lazy"
      decoding="async"
    />
  </div>

  <div
    class="w-full flex justify-center items-center z-20 h-[20%] pb-1 absolute bottom-0"
  >
    <img
      src={getAssetUrl("/acm-logo-black.webp")}
      alt="CSI-VIT Logo"
      class="h-full object-cover"
      loading="lazy"
      decoding="async"
    />
  </div>

  <!-- Member Info -->
  <div class="flex items-center w-full h-full text-center z-10">
    <div class="h-full w-2/5 flex items-end overflow-hidden">
      <img
        src={imageUrl}
        alt={fullName}
        class="h-full w-full object-cover object-top"
        loading="lazy"
        decoding="async"
      />
    </div>

    <div
      class="w-3/5 z-20 text-left flex flex-col justify-center mb-10 gap-1.5 pl-8 pr-4"
    >
      <div class="">
        <h3
          style="font-family: 'PolySans Bold';"
          class="text-[100%] text-gray-900 m-0"
        >
          {title}
          {fullName}
        </h3>
        <p
          style="font-family: PolySans Slim;"
          class={!isW
            ? "text-[80%] text-primary-orange m-0"
            : "text-[80%] text-[#FF007A] m-0"}
        >
          {position}
        </p>
      </div>

      <!-- Social Links -->
      <div class="flex justify-center gap-3 mt-2">
        {
          linkedinUrl && (
            <a
              href={linkedinUrl}
              target="_blank"
              rel="noopener noreferrer"
              class="hover:cursor-pointer"
            >
              <img
                src={
                  isW
                    ? getAssetUrl("/linkedin-pink.webp")
                    : getAssetUrl("/linkedin-orange.webp")
                }
                alt="LinkedIn"
                class="w-[5vw] h-[5vw] sm:w-[2vw] sm:h-[2vw] lg:w-[1.7vw] lg:h-[1.7vw]"
                loading="lazy"
                decoding="async"
              />
            </a>
          )
        }

        {
          googleScholarUrl && (
            <a
              href={googleScholarUrl}
              target="_blank"
              rel="noopener noreferrer"
              class="hover:cursor-pointer"
            >
              <img
                src={
                  isW
                    ? getAssetUrl("/google-scholar-pink.webp")
                    : getAssetUrl("/google-scholar-orange.webp")
                }
                alt="Google Scholar"
                class="w-[5vw] h-[5vw] sm:w-[2vw] sm:h-[2vw] lg:w-[1.7vw] lg:h-[1.7vw]"
                loading="lazy"
                decoding="async"
              />
            </a>
          )
        }

        {
          githubUrl && (
            <a
              href={githubUrl}
              target="_blank"
              rel="noopener noreferrer"
              class="hover:cursor-pointer"
            >
              <img
                src={
                  isW
                    ? getAssetUrl("/github-pink.webp")
                    : getAssetUrl("/github-orange.webp")
                }
                alt="GitHub"
                class="w-[5vw] h-[5vw] sm:w-[2vw] sm:h-[2vw] lg:w-[1.7vw] lg:h-[1.7vw]"
                loading="lazy"
                decoding="async"
              />
            </a>
          )
        }
      </div>
    </div>
  </div>
</div>

<style>
  /* Local font-face definitions removed in favor of globals.css */
</style>
